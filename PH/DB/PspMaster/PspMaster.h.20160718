#ifndef _PSPMASTER_H_
#define _PSPMASTER_H_

#include "myhash.h"
#ifdef __cplusplus
extern "C" {
#endif

int Add(const hash_t *);
int Delete(const unsigned char* client_id);
int GetPspMaster(const char* csClientId,
                hash_t* hRec);

int Update(const hash_t *hRls);

int GetPspMasterMode(const char *csClientId,
			hash_t * hRec);

int GetClientIdByClientName(const char* csClientName,
			       char* csClientId);

#ifdef __cplusplus
}
#endif

#endif
