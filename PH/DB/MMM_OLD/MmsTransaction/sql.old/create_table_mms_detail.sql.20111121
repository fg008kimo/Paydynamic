ALTER TABLE MMS_TXN_DETAIL
 DROP PRIMARY KEY CASCADE;

DROP TABLE MMS_TXN_DETAIL CASCADE CONSTRAINTS;

CREATE TABLE MMS_TXN_DETAIL
(
  MTD_TXN_ID                 VARCHAR2(16 BYTE),
  MTD_MERCHANT_ID            VARCHAR2(15 BYTE),
  MTD_PSP_ID                 VARCHAR2(10 BYTE),
  MTD_TRANSMISSION_DATETIME  VARCHAR2(14 BYTE),
  MTD_COUNTRY                VARCHAR2(3 BYTE),
  MTD_VERSION                VARCHAR2(2 BYTE),
  MTD_ENCRYPT_TYPE           CHAR(1 BYTE),
  MTD_CREATE_DATE            DATE,
  MTD_CREATE_USER            VARCHAR2(20 BYTE),
  MTD_UPDATE_DATE            DATE,
  MTD_UPDATE_USER            VARCHAR2(20 BYTE)
)
TABLESPACE USER_DATA
RESULT_CACHE (MODE DEFAULT)
PCTUSED    0
PCTFREE    10
INITRANS   1
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
LOGGING 
NOCOMPRESS 
NOCACHE
NOPARALLEL
MONITORING;


CREATE UNIQUE INDEX MMS_TXN_DETAIL_PK ON MMS_TXN_DETAIL
(MTD_TXN_ID)
LOGGING
TABLESPACE USER_IDX
PCTFREE    10
INITRANS   2
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
NOPARALLEL;


ALTER TABLE MMS_TXN_DETAIL ADD (
  CONSTRAINT MMS_TXN_DETAIL_PK
  PRIMARY KEY
  (MTD_TXN_ID)
  USING INDEX MMS_TXN_DETAIL_PK
  ENABLE VALIDATE);

