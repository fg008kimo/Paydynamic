/*
Partnerdelight (c)2014. All rights reserved. No part of this software may be reproduced in any form without written permission
of an authorized representative of Partnerdelight.

Change Description                                 Change Date             Change By
-------------------------------                    ------------            --------------
Init Version                                       2014/11/21              David Wong
*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <sqlca.h>
#include "common.h"
#include "utilitys.h"
#include "internal.h"
#include "dbutility.h"
#include "OLStmtMatchRule.h"

#define SQLCA_STORAGE_CLASS extern
#define SQLCODE sqlca.sqlcode


char cDebug;

void OLStmtMatchRule(char cdebug)
{
	cDebug = cdebug;
}


/*
int GetStmtMatchRule(const hash_t *hContext, recordset_t *myRec)
{
	int iRet = PD_OK;
	int iCnt = 0;
	int iTmp;

	hash_t *myHash;

	EXEC SQL WHENEVER SQLERROR GOTO getstmtmatchrule_error;
	EXEC SQL WHENEVER NOTFOUND CONTINUE;

	EXEC SQL BEGIN DECLARE SECTION;
		int hv_rule_id;

		int v_sub_rule_id;
		int v_action_seq;
		varchar v_action_type[PD_ENGINE_ACTION_LEN + 1];
		int v_action_filter_id;
		int v_exit_on_error;

		short ind_sub_rule_id = -1;
		short ind_action_seq = -1;
		short ind_action_type = -1;
		short ind_action_filter_id = -1;
		short ind_exit_on_error = -1;
	EXEC SQL END DECLARE SECTION;

	if (GetField_Int(hContext, "rule_id", &iTmp)) {
		hv_rule_id = iTmp;
DEBUGLOG(("GetStmtMatchRule rule_id = [%d]\n", hv_rule_id));
	} else {
DEBUGLOG(("GetStmtMatchRule rule_id not found!\n"));
		iRet = PD_ERR;
	}

	EXEC SQL DECLARE c_getstmtmatchrule CURSOR FOR
		SELECT	OSMR_SUB_RULE_ID,
			OSMR_ACTION_SEQ,
			OSMR_ACTION_TYPE,
			OSMR_ACTION_FILTER_ID,
			OSMR_EXIT_ON_ERROR
		FROM	OL_STMT_MATCH_RULE
		WHERE	OSMR_RULE_ID = :hv_rule_id
		AND	OSMR_DISABLED = 0
		ORDER BY	OSMR_SUB_RULE_ID,
				OSMR_ACTION_SEQ;

	if (iRet == PD_OK) {
		EXEC SQL OPEN c_getstmtmatchrule;

		for (;;) {
			EXEC SQL FETCH c_getstmtmatchrule
			INTO	:v_sub_rule_id:ind_sub_rule_id,
					:v_action_seq:ind_action_seq,
					:v_action_type:ind_action_type,
					:v_action_filter_id:ind_action_filter_id,
					:v_exit_on_error:ind_exit_on_error;

			if (SQLCODE == SQL_NOT_FOUND) {
				break;
			}

			iCnt++;

			myHash = (hash_t*) malloc (sizeof(hash_t));
			hash_init(myHash, 0);

			// sub_rule_id
			if (ind_sub_rule_id >= 0) {
				PutField_Int(myHash, "sub_rule_id", v_sub_rule_id);
DEBUGLOG(("GetStmtMatchRule sub_rule_id = [%d]\n", v_sub_rule_id));
			}

			// action_seq
			if (ind_action_seq >= 0) {
				PutField_Int(myHash, "action_seq", v_action_seq);
DEBUGLOG(("GetStmtMatchRule action_seq = [%d]\n", v_action_seq));
			}

			// action_type
			if (ind_action_type >= 0) {
				v_action_type.arr[v_action_type.len] = '\0';
				PutField_CString(myHash, "action_type", (const char*)v_action_type.arr);
DEBUGLOG(("GetStmtMatchRule action_type = [%s]\n", (const char*)v_action_type.arr));
			}

			// action_filter_id
			if (ind_action_filter_id >= 0) {
				PutField_Int(myHash, "action_filter_id", v_action_filter_id);
DEBUGLOG(("GetStmtMatchRule action_filter_id = [%d]\n", v_action_filter_id));
			}

			// exit_on_error
			if (ind_exit_on_error >= 0) {
				PutField_Int(myHash, "exit_on_error", v_exit_on_error);
DEBUGLOG(("GetStmtMatchRule exit_on_error = [%d]\n", v_exit_on_error));
			}

			RecordSet_Add(myRec, myHash);
		}

		EXEC SQL CLOSE c_getstmtmatchrule;

		if (iCnt == 0) {
DEBUGLOG(("GetStmtMatchRule not found\n"));
ERRLOG("OLStmtMatchRule:: GetStmtMatchRule not found\n");
			iRet = PD_ERR;
		}
	}

DEBUGLOG(("GetStmtMatchRule Normal Exit! iRet = [%d]\n", iRet));
	return iRet;

getstmtmatchrule_error:
DEBUGLOG(("getstmtmatchrule_error code %d\n", sqlca.sqlcode));
DEBUGLOG(("\n%.*s\n", sqlca.sqlerrm.sqlerrml, sqlca.sqlerrm.sqlerrmc));
ERRLOG("OLStmtMatchRule getstmtmatchrule_error code %d\n", sqlca.sqlcode);
ERRLOG("\n%.*s\n", sqlca.sqlerrm.sqlerrml, sqlca.sqlerrm.sqlerrmc);
	EXEC SQL WHENEVER SQLERROR CONTINUE;
	EXEC SQL CLOSE c_getstmtmatchrule;
	return PD_ERR;
}
*/


/*
int GetStmtMatchRule(const hash_t *hContext, recordset_t *myRec)
{
	int iRet = PD_OK;
	int iCnt = 0;
	int iTmp;

	hash_t *myHash;

	EXEC SQL WHENEVER SQLERROR GOTO getstmtmatchrule_error;
	EXEC SQL WHENEVER NOTFOUND CONTINUE;

	EXEC SQL BEGIN DECLARE SECTION;
		int hv_rule_id;
		int v_sub_rule_id;
		short ind_sub_rule_id = -1;
	EXEC SQL END DECLARE SECTION;

	if (GetField_Int(hContext, "rule_id", &iTmp)) {
		hv_rule_id = iTmp;
DEBUGLOG(("GetStmtMatchRule rule_id = [%d]\n", hv_rule_id));
	} else {
DEBUGLOG(("GetStmtMatchRule rule_id not found!\n"));
		iRet = PD_ERR;
	}

	EXEC SQL DECLARE c_getstmtmatchrule CURSOR FOR
		SELECT	DISTINCT(OSMR_SUB_RULE_ID)
		FROM	OL_STMT_MATCH_RULE
		WHERE	OSMR_RULE_ID = :hv_rule_id
		AND	OSMR_DISABLED = 0
		ORDER BY	OSMR_SUB_RULE_ID;

	if (iRet == PD_OK) {
		EXEC SQL OPEN c_getstmtmatchrule;

		for (;;) {
			EXEC SQL FETCH c_getstmtmatchrule
			INTO	:v_sub_rule_id:ind_sub_rule_id;

			if (SQLCODE == SQL_NOT_FOUND) {
				break;
			}

			iCnt++;

			myHash = (hash_t*) malloc (sizeof(hash_t));
			hash_init(myHash, 0);

			// sub_rule_id
			if (ind_sub_rule_id >= 0) {
				PutField_Int(myHash, "sub_rule_id", v_sub_rule_id);
DEBUGLOG(("GetStmtMatchRule sub_rule_id = [%d]\n", v_sub_rule_id));
			}

			RecordSet_Add(myRec, myHash);
		}

		EXEC SQL CLOSE c_getstmtmatchrule;

		if (iCnt == 0) {
DEBUGLOG(("GetStmtMatchRule not found\n"));
ERRLOG("OLStmtMatchRule:: GetStmtMatchRule not found\n");
			iRet = PD_ERR;
		}
	}

DEBUGLOG(("GetStmtMatchRule Normal Exit! iRet = [%d]\n", iRet));
	return iRet;

getstmtmatchrule_error:
DEBUGLOG(("getstmtmatchrule_error code %d\n", sqlca.sqlcode));
DEBUGLOG(("\n%.*s\n", sqlca.sqlerrm.sqlerrml, sqlca.sqlerrm.sqlerrmc));
ERRLOG("OLStmtMatchRule getstmtmatchrule_error code %d\n", sqlca.sqlcode);
ERRLOG("\n%.*s\n", sqlca.sqlerrm.sqlerrml, sqlca.sqlerrm.sqlerrmc);
	EXEC SQL WHENEVER SQLERROR CONTINUE;
	EXEC SQL CLOSE c_getstmtmatchrule;
	return PD_ERR;
}
*/


int GetStmtMatchSubRule(const hash_t *hContext, recordset_t *myRec)
{
	int iRet = PD_OK;
	int iCnt = 0;
	int iTmp;

	hash_t *myHash;

	EXEC SQL WHENEVER SQLERROR GOTO getstmtmatchsubrule_error;
	EXEC SQL WHENEVER NOTFOUND CONTINUE;

	EXEC SQL BEGIN DECLARE SECTION;
		int hv_rule_id;
		int hv_sub_rule_id;

		varchar v_action_type[PD_ENGINE_ACTION_LEN + 1];
		int v_action_filter_id;
		int v_exit_on_error;

		short ind_action_type = -1;
		short ind_action_filter_id = -1;
		short ind_exit_on_error = -1;
	EXEC SQL END DECLARE SECTION;

	if (GetField_Int(hContext, "rule_id", &iTmp)) {
		hv_rule_id = iTmp;
DEBUGLOG(("GetStmtMatchSubRule rule_id = [%d]\n", hv_rule_id));
	} else {
DEBUGLOG(("GetStmtMatchSubRule rule_id not found!\n"));
		iRet = PD_ERR;
	}

	if (GetField_Int(hContext, "sub_rule_id", &iTmp)) {
		hv_sub_rule_id = iTmp;
DEBUGLOG(("GetStmtMatchSubRule sub_rule_id = [%d]\n", hv_sub_rule_id));
	} else {
DEBUGLOG(("GetStmtMatchSubRule sub_rule_id not found!\n"));
		iRet = PD_ERR;
	}

	EXEC SQL DECLARE c_getstmtmatchsubrule CURSOR FOR
		SELECT	OSMR_ACTION_TYPE,
			OSMR_ACTION_FILTER_ID,
			OSMR_EXIT_ON_ERROR
		FROM	OL_STMT_MATCH_RULE
		WHERE	OSMR_RULE_ID = :hv_rule_id
		AND	OSMR_SUB_RULE_ID = :hv_sub_rule_id
		AND	OSMR_DISABLED = 0
		ORDER BY	OSMR_ACTION_SEQ;

	if (iRet == PD_OK) {
		EXEC SQL OPEN c_getstmtmatchsubrule;

		for (;;) {
			EXEC SQL FETCH c_getstmtmatchsubrule
			INTO	:v_action_type:ind_action_type,
					:v_action_filter_id:ind_action_filter_id,
					:v_exit_on_error:ind_exit_on_error;

			if (SQLCODE == SQL_NOT_FOUND) {
				break;
			}

			iCnt++;

			myHash = (hash_t*) malloc (sizeof(hash_t));
			hash_init(myHash, 0);

			// action_type
			if (ind_action_type >= 0) {
				v_action_type.arr[v_action_type.len] = '\0';
				PutField_CString(myHash, "action_type", (const char*)v_action_type.arr);
DEBUGLOG(("GetStmtMatchSubRule action_type = [%s]\n", (const char*)v_action_type.arr));
			}

			// action_filter_id
			if (ind_action_filter_id >= 0) {
				PutField_Int(myHash, "action_filter_id", v_action_filter_id);
DEBUGLOG(("GetStmtMatchSubRule action_filter_id = [%d]\n", v_action_filter_id));
			}

			// exit_on_error
			if (ind_exit_on_error >= 0) {
				PutField_Int(myHash, "exit_on_error", v_exit_on_error);
DEBUGLOG(("GetStmtMatchSubRule exit_on_error = [%d]\n", v_exit_on_error));
			}

			RecordSet_Add(myRec, myHash);
		}

		EXEC SQL CLOSE c_getstmtmatchsubrule;

		if (iCnt == 0) {
DEBUGLOG(("GetStmtMatchSubRule not found\n"));
ERRLOG("OLStmtMatchRule:: GetStmtMatchSubRule not found\n");
			iRet = PD_ERR;
		}
	}

DEBUGLOG(("GetStmtMatchSubRule Normal Exit! iRet = [%d]\n", iRet));
	return iRet;

getstmtmatchsubrule_error:
DEBUGLOG(("getstmtmatchsubrule_error code %d\n", sqlca.sqlcode));
DEBUGLOG(("\n%.*s\n", sqlca.sqlerrm.sqlerrml, sqlca.sqlerrm.sqlerrmc));
ERRLOG("OLStmtMatchRule getstmtmatchsubrule_error code %d\n", sqlca.sqlcode);
ERRLOG("\n%.*s\n", sqlca.sqlerrm.sqlerrml, sqlca.sqlerrm.sqlerrmc);
	EXEC SQL WHENEVER SQLERROR CONTINUE;
	EXEC SQL CLOSE c_getstmtmatchsubrule;
	return PD_ERR;
}

