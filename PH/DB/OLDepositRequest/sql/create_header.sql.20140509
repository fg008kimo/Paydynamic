ALTER TABLE OL_DEPOSIT_REQUEST_HEADER
 DROP PRIMARY KEY CASCADE;

DROP TABLE OL_DEPOSIT_REQUEST_HEADER CASCADE CONSTRAINTS;

CREATE TABLE OL_DEPOSIT_REQUEST_HEADER
(
  OLDH_FILE_ID              NUMBER(16),
  OLDH_FILENAME             VARCHAR2(100 BYTE),
  OLDH_MERCHANT_ID          VARCHAR2(15 BYTE),
  OLDH_ACCEPT_COUNT         INTEGER,
  OLDH_ACCEPT_AMOUNT        NUMBER(14,2),
  OLDH_TOTAL_COUNT          INTEGER,
  OLDH_STATUS               CHAR(1 BYTE),
  OLDH_PARTY                CHAR(1 BYTE),
  OLDH_CREATE_TIMESTAMP     DATE,
  OLDH_CREATE_USER          VARCHAR2(20 BYTE),
  OLDH_UPDATE_TIMESTAMP     DATE,
  OLDH_UPDATE_USER          VARCHAR2(20 BYTE)
)
TABLESPACE USER_DATA
RESULT_CACHE (MODE DEFAULT)
PCTUSED    0
PCTFREE    10
INITRANS   1
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
LOGGING 
NOCOMPRESS 
NOCACHE
NOPARALLEL
MONITORING;

CREATE UNIQUE INDEX OL_DEPOSIT_REQ_HEADER_IDX01 ON OL_DEPOSIT_REQUEST_HEADER
(OLDH_MERCHANT_ID, OLDH_FILENAME)
LOGGING
TABLESPACE USER_IDX
PCTFREE    10
INITRANS   2
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
NOPARALLEL;

CREATE INDEX OL_DEPOSIT_REQ_HEADER_IDX02 ON OL_DEPOSIT_REQUEST_HEADER
(OLDH_FILENAME)
LOGGING
TABLESPACE USER_IDX
PCTFREE    10
INITRANS   2
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
NOPARALLEL;

CREATE UNIQUE INDEX OL_DEPOSIT_REQUEST_HEADER_PK ON OL_DEPOSIT_REQUEST_HEADER
(OLDH_FILE_ID)
LOGGING
TABLESPACE USER_IDX
PCTFREE    10
INITRANS   2
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
NOPARALLEL;


ALTER TABLE OL_DEPOSIT_REQUEST_HEADER ADD (
  CONSTRAINT OL_DEPOSIT_REQUEST_HEADER_PK
  PRIMARY KEY
  (OLDH_FILE_ID)
  USING INDEX OL_DEPOSIT_REQUEST_HEADER_PK
  ENABLE VALIDATE);

ALTER TABLE OL_DEPOSIT_REQUEST_HEADER ADD (
  CONSTRAINT OL_DEPOSIT_REQUEST_HEADER_FK01
  FOREIGN KEY (OLDH_MERCHANT_ID)
  REFERENCES OL_MERCH_DETAIL (MD_MERCHANT_ID)
  ENABLE VALIDATE,
  CONSTRAINT OL_DEPOSIT_REQUEST_HEADER_FK02
  FOREIGN KEY (OLDH_STATUS)
  REFERENCES OL_DEF_FILE_STATUS (DFS_STATUS)
  ENABLE VALIDATE);
