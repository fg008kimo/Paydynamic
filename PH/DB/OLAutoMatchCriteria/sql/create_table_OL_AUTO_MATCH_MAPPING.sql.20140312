ALTER TABLE OL_AUTO_MATCH_MAPPING
 DROP PRIMARY KEY CASCADE;

DROP TABLE OL_AUTO_MATCH_MAPPING CASCADE CONSTRAINTS;

CREATE TABLE OL_AUTO_MATCH_MAPPING
(
  AMM_MERCHANT_ID       VARCHAR2(15 BYTE),
  AMM_POOL_ID           INTEGER,
  AMM_PRIORITY          INTEGER,
  AMM_MAP_DESC          VARCHAR2(50 BYTE),
  AMM_DISABLED          INTEGER                 DEFAULT 0,
  AMM_CREATE_TIMESTAMP  DATE                    DEFAULT sysdate,
  AMM_CREATE_USER       VARCHAR2(20 BYTE),
  AMM_UPDATE_TIMESTAMP  DATE                    DEFAULT sysdate,
  AMM_UPDATE_USER       VARCHAR2(20 BYTE)
)
TABLESPACE USER_DATA
RESULT_CACHE (MODE DEFAULT)
PCTUSED    0
PCTFREE    10
INITRANS   1
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MAXSIZE          UNLIMITED
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
LOGGING 
NOCOMPRESS 
NOCACHE
NOPARALLEL
MONITORING;


CREATE UNIQUE INDEX OL_AUTO_MATCH_MAPPING_PK ON OL_AUTO_MATCH_MAPPING
(AMM_MERCHANT_ID, AMM_POOL_ID)
LOGGING
TABLESPACE USER_DATA
PCTFREE    10
INITRANS   2
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MAXSIZE          UNLIMITED
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
NOPARALLEL;


ALTER TABLE OL_AUTO_MATCH_MAPPING ADD (
  CONSTRAINT OL_AUTO_MATCH_MAPPING_PK
  PRIMARY KEY
  (AMM_MERCHANT_ID, AMM_POOL_ID)
  USING INDEX OL_AUTO_MATCH_MAPPING_PK
  ENABLE VALIDATE);

ALTER TABLE OL_AUTO_MATCH_MAPPING ADD (
  CONSTRAINT OL_AUTO_MATCH_MAPPING_FK01 
  FOREIGN KEY (AMM_MERCHANT_ID) 
  REFERENCES OL_MERCH_DETAIL (MD_MERCHANT_ID)
  ENABLE VALIDATE);

