ALTER TABLE OL_TXN_ENGINE_ACTION_LIST
 DROP PRIMARY KEY CASCADE;

DROP TABLE OL_TXN_ENGINE_ACTION_LIST CASCADE CONSTRAINTS;

CREATE TABLE OL_TXN_ENGINE_ACTION_LIST
(
  EAL_ACTION_DETAIL_ID   INTEGER,
  EAL_ACTION_DESC        VARCHAR2(50 BYTE),
  EAL_ACTION_TYPE        INTEGER,
  EAL_ACTION_LEVEL       VARCHAR2(10 BYTE),
  EAL_AFTER_STATUS_RULE  INTEGER,
  EAL_BAL_RULE           INTEGER,
  EAL_TXN_AMT_TAG        VARCHAR2(25 BYTE),
  EAL_LINKAGE            VARCHAR2(10 BYTE),
  EAL_CREATE_TIMESTAMP   DATE                   DEFAULT sysdate,
  EAL_CREATE_USER        VARCHAR2(20 BYTE),
  EAL_UPDATE_TIMESTAMP   DATE                   DEFAULT sysdate,
  EAL_UPDATE_USER        VARCHAR2(20 BYTE)
)
TABLESPACE USER_DATA
RESULT_CACHE (MODE DEFAULT)
PCTUSED    0
PCTFREE    10
INITRANS   1
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MAXSIZE          UNLIMITED
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
LOGGING 
NOCOMPRESS 
NOCACHE
NOPARALLEL
MONITORING;


CREATE UNIQUE INDEX OL_TXN_ENGINE_ACTION_LIST_PK ON OL_TXN_ENGINE_ACTION_LIST
(EAL_ACTION_DETAIL_ID)
LOGGING
TABLESPACE USER_IDX
PCTFREE    10
INITRANS   2
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MAXSIZE          UNLIMITED
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
NOPARALLEL;


ALTER TABLE OL_TXN_ENGINE_ACTION_LIST ADD (
  CONSTRAINT OL_TXN_ENGINE_ACTION_LIST_PK
  PRIMARY KEY
  (EAL_ACTION_DETAIL_ID)
  USING INDEX OL_TXN_ENGINE_ACTION_LIST_PK
  ENABLE VALIDATE);

ALTER TABLE OL_TXN_ENGINE_ACTION_LIST ADD (
  CONSTRAINT OL_TXN_ENGINE_ACTION_LIST_FK01 
  FOREIGN KEY (EAL_ACTION_LEVEL) 
  REFERENCES OL_DEF_ENGINE_PARTY (EP_TYPE)
  ENABLE VALIDATE,
  CONSTRAINT OL_TXN_ENGINE_ACTION_LIST_FK02 
  FOREIGN KEY (EAL_LINKAGE) 
  REFERENCES OL_DEF_ENGINE_PARTY (EP_TYPE)
  ENABLE VALIDATE);

