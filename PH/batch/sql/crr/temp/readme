to-do-list

1. prepare TMP_CRR_CARRY_OLD_BALANCE_DATA_TABLE.ldr
2. change the txn_date, year, month in sp_crr_jnl_insert_from_cms
2. run import_from_cms.sh
3. check if all success (not if tmp table, function, import_from_cms.log will not be removed, check tmp table for each record failed)


logic:

import_from_cms.sh will use TMP_CRR_CARRY_OLD_BALANCE_DATA_TABLE.ctl
   to insert .ldr into table TMP_CRR_CARRY_OLD_BALANCE (check this tmp table to see if data inserting correctly)
and then use function tmp_sp_crr_jnl_insert_from_cms
   to insert into table CRR_JNL_HEADER, CRR_JNL_DETAIL, CRR_JNL_DETAIL_AMT, CRR_JNL_CHANGE_LOG
finally update the status of record in table TMP_CRR_CARRY_OLD_BALANCE
   and output result
