/*
PDProTech (c)2010. All rights reserved. No part of this software may be reproduced in any form without written permission
of an authorized representative of PDProTech.

Change Description                            Change Date     Change By
------------------------------------------    ------------    --------------
Init Version                                  2020/02/26      Michael Chow
*/


#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <ctype.h>
#include <sqlca.h>
#include <sys/types.h>
#include <time.h>
#include "batchcommon.h"
#include "common.h"
#include "utilitys.h"
#include "myhash.h"
#include "numutility.h"
#include "ObjPtr.h"



OBJPTR(DB);
OBJPTR(Txn);

static char cDebug = 'Y';

int parse_arg(int argc,char **argv);

int process_data(); 
					

int batch_init(int argc, char* argv[])
{
	return SUCCESS;
}

int batch_proc(int argc, char* argv[])
{
	int iRet;

	iRet = process_data();

	return iRet;
}

int batch_terminate(int argc, char* argv[])
{
	return SUCCESS;
}

int process_data()
{               
	
	int     iRet = SUCCESS;


	
	hash_t* hResult;
	hResult = (hash_t*) malloc (sizeof(hash_t));
	hash_init(hResult,0);

	hash_t* hRequest;
	hRequest = (hash_t*) malloc (sizeof(hash_t));
	hash_init(hRequest,0);
	char csProvId[30];


	


	PutField_CString(hRequest, "action", "A");
	PutField_CString(hRequest, "prov_name", "test18");
	PutField_CString(hRequest, "prov_code", "test18");
	PutField_CString(hRequest, "dsi_prov_flr_name", "TC41");
	PutField_CString(hRequest, "poa_prov_flr_name", "TC41");
	PutField_CString(hRequest, "int_prov_flr_name", "TC@");
	PutField_CString(hRequest, "txn_ccy", "CNY");
	PutField_CString(hRequest, "type", "G");
	PutField_CString(hRequest, "po_split_limit", "1000");
	PutField_CString(hRequest, "add_user", "SYSTEM");
	PutField_CString(hRequest, "product_code", "OFF");

	//if (GetField_CString(hRequest, "prov_name", &csProvName)) {
	//if (GetField_CString(hRequest, "prov_code", &csProvCode)) {
	//if (GetField_CString(hRequest, "dsi_prov_flr_name", &csDsiProvFlrName)) {
	//if (GetField_CString(hRequest, "poa_prov_flr_name", &csPoaProvFlrName)) {
	//if (GetField_CString(hRequest, "int_prov_flr_name", &csIntProvFlrName)) {
	//if (GetField_CString(hRequest, "txn_ccy", &csCcy)) {
	//if (GetField_CString(hRequest, "type", &csTmp)) {
	//if (GetField_CString(hRequest, "po_split_limit", &csTmp)) {
	//if (GetField_CString(hRequest, "add_user", &csUser)) {
	//if(GetField_CString(hRequest, "product_code", &csCrrProduct))


	TxnObjPtr = CreateObj(TxnPtr,"TxnOmtByUsTCP","Authorize");
	iRet = (unsigned long)(TxnObjPtr)(hRequest,hRequest,hResult);
DEBUGLOG((" TCP RetCode: [%d]!!\n", iRet));


	hash_destroy(hResult);
	FREE_ME(hResult);

	hash_destroy(hRequest);
	FREE_ME(hRequest);

	return PD_ERR;		//test
		

		
	return iRet;

}


int parse_arg(int argc,char **argv)
{

	return SUCCESS;
}

